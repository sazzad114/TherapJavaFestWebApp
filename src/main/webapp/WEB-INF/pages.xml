<?xml version="1.0" encoding="UTF-8"?>
<pages xmlns="http://jboss.com/products/seam/pages"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://jboss.com/products/seam/pages http://jboss.com/products/seam/pages-2.0.xsd">

    <page view-id="/register.xhtml">
        <navigation>
            <rule if-outcome="success">
                <redirect view-id="/emailsent.xhtml"/>

            </rule>
        </navigation>
    </page>

    <page view-id="/home.xhtml" action="#{identity.isLoggedIn}">

        <navigation from-action="#{identity.isLoggedIn}">
            <rule if-outcome="true">
                <redirect view-id="/greeting.xhtml"/>
            </rule>
            <rule if-outcome="false">
                <render view-id="/home.xhtml"/>
            </rule>
        </navigation>

    </page>

      <page view-id="/greeting.xhtml" login-required="true">

        <!--<navigation>



        </navigation>-->

    </page>

    <page view-id="/changePassword.xhtml"
          login-required="true">
        <navigation>
            <rule if="#{passwordChange.changed}">
                <redirect view-id="/home.xhtml"/>
            </rule>
        </navigation>

    </page>

    <page view-id="*">

        <navigation from-action="#{identity.logout}">
            <redirect view-id="/home.xhtml"/>
        </navigation>

    </page>

    <exception class="org.jboss.seam.security.NotLoggedInException">
        <redirect view-id="/home.xhtml">
            <message severity="warn">You must be logged in to use this feature</message>
        </redirect>
    </exception>

    <exception class="javax.faces.application.ViewExpiredException">
        <redirect view-id="/home.xhtml">
            <message severity="warn">Session expired, please log in again</message>
        </redirect>
    </exception>

</pages>
